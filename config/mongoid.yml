development:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>

test:
  sessions:
    default:
      uri: "mongodb://<%= ENV['MONGO_TEST_USER'] %>:<%= ENV['MONGO_TEST_PASSWORD'] %>@linus.mongohq.com:10098/toy_tracker_test"

production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>
